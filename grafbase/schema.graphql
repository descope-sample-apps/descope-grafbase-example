schema
  @auth(
    providers: [{ type: oidc, issuer: "{{ env.DESCOPE_ISSUER_URL }}" }]
    rules: [{ allow: private }]
  ) {
  query: Query
}

type Message @model {
  author: String!
  body: String!
}
